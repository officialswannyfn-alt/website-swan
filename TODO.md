# Gallery Fix - TODO

- [x] 1. Edit `index.html`: Link `style.css` and remove duplicate inline `<style>` block
- [x] 2. Edit `index.html`: URL-encode all image `src` paths with spaces/special characters
- [x] 3. Edit `index.html`: Add background color to particles in JS
- [x] 4. Edit `style.css`: Add background color to `.p` particle class
- [x] 5. Clean up stray `</style>` tag at end of `index.html`
- [ ] 6. Verify gallery displays correctly
